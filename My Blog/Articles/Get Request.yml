info:
  name: Get Request
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/articles/{{articleId}}
  auth: inherit

runtime:
  variables:
    - name: articleId
      value: "1"
      disabled: true
    - name: articleId
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
